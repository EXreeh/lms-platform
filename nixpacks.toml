# Used when Railway/Nixpacks builds from repo root
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build:railway"]

[start]
cmd = "npm run migrate:deploy -w database && npm run start -w backend"
