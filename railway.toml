# CognitiaX AI LMS — deploy from monorepo root (not backend/ alone).
# In Railway: set Root Directory to repository root, or use these commands.

[build]
buildCommand = "npm ci && npm run build:railway"

[deploy]
startCommand = "npm run migrate:deploy -w database && npm run start -w backend"
restartPolicyType = "on_failure"
